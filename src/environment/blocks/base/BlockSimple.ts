import {BlockFactory, Geometry, BlockData} from "../Block";

/**
 * Block simple is just a generic block which
 * - Uses textures from an atlas
 * - Uses the "base:mat:simple" 
 */
class BlockSimple extends BlockFactory{
	constructor(){
		super();
	}
}